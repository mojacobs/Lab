@model List<RideSharePlus.Models.Ride>

<div id="sametown">
        @if (Model.Count == 0)
        {
            <p>No Results!</p>
        }
        <ul>
            @foreach (var item in Model)
            {
                <li>@item.StudentEmail</li>
            }
        </ul>
    </div>